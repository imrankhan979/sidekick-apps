{% style %}
    .position-{{ block.id }}-top{
        top: {{ block.settings.top_offset }}%;
    }
    .position-{{ block.id }}-left{
        left: {{ block.settings.left_offset }}%;
    }
    .block-shape-img{
        position: absolute;
    }
{% endstyle %}
<img 
  src="{{ block.settings.image | image_url: width: 1500 }}" 
  srcset="{{ block.settings.image | image_url: width: 165 }} 165w, 
          {{ block.settings.image | image_url: width: 360 }} 360w, 
          {{ block.settings.image | image_url: width: 535 }} 535w, 
          {{ block.settings.image | image_url: width: 750 }} 750w, 
          {{ block.settings.image | image_url: width: 1070 }} 1070w, 
          {{ block.settings.image | image_url: width: 1500 }} 1500w" 
        class="block-shape-img" 
        width="1500" 
        height="556" 
        loading="lazy" 
        style="top: {{ block.settings.top_offset }}%;  left: {{ block.settings.left_offset }}%; max-width: {{ block.settings.max_width }}%">
{% schema %}
    {
        "name": "t:blocks.shape_image.name",
        "settings": [
            {
                "type": "image_picker",
                "id": "image",
                "label": "t:blocks.shape_image.settings.image.label"
            },
            {
                "type": "range",
                "id": "max_width",
                "label": "t:blocks.shape_image.settings.max_width.label",
                "default": 80,
                "min": 0,
                "max": 100,
                "step": 1,
                "unit": "%"
            },
            {
                "type": "range",
                "id": "top_offset",
                "label": "t:blocks.shape_image.settings.top_offset.label",
                "default": 0,
                "min": -100,
                "max": 100,
                "step": 2,
                "unit": "%"
            },
            {
                "type": "range",
                "id": "left_offset",
                "label": "t:blocks.shape_image.settings.left_offset.label",
                "default": 0,
                "min": -100,
                "max": 100,
                "step": 2,
                "unit": "%"
            }
        ],
        "presets": [
            {
                "name": "t:blocks.shape_image.presets.name"
            }
        ]
    }
{% endschema %}